const router = require("express").Router();
const db = require("../models");

//GET routes

router.get("/api/dash", (req, res) => {});

router.get("/api/dash/:id", (req, res) => {});

//POST route

router.post("/api/dash", (req, res) => {});

//DELETE route

router.delete("/api/dash/:id", (req, res) => {});

module.exports = router;
